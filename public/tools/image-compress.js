async function compress(){const f=document.getElementById('file').files[0];if(!f)return;document.getElementById('orig-size').textContent=(f.size/1024).toFixed(1)+' KB';const img=await createImageBitmap(f);const c=document.getElementById('canvas');c.width=img.width;c.height=img.height;c.getContext('2d').drawImage(img,0,0);c.toBlob(b=>{document.getElementById('new-size').textContent=(b.size/1024).toFixed(1)+' KB';document.getElementById('result-area').classList.remove('hidden');const u=URL.createObjectURL(b);const p=document.getElementById('preview');p.src=u;p.classList.remove('hidden');},'image/webp',0.6);}