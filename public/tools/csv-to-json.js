function convert(){try{const lines=document.getElementById('csv').value.trim().split('\n');const headers=lines[0].split(',').map(h=>h.trim());const result=lines.slice(1).filter(l=>l.trim()).map(line=>{const vals=line.split(',');const obj={};headers.forEach((h,i)=>obj[h]=vals[i]?vals[i].trim():'');return obj;});document.getElementById('json').value=JSON.stringify(result,null,2);}catch(e){document.getElementById('json').value='错误: '+e.message;}}